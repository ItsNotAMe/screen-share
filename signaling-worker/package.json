{
  "name": "screenshare-signaling-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal Cloudflare Worker signaling backend for ScreenShare UDP peer discovery.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.8.0",
    "wrangler": "^4.0.0"
  }
}
